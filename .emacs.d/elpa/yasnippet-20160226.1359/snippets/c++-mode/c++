# -*- mode: snippet -*-
# name: c++
# key: c++
# --
#include <iostream>
#include <cstdlib>
$2
#define for0(i, n) for(int (i) = 0 ; (i)<(n) ; (i)++)
#define for1(i, n) for(int (i) = 1 ; (i)<(n) ; (i)++)
#define forn(i, k, n) for(int (i) = (k) ; (i)<(n) ; (i)++)
#define forv(i, n, k) for(int (i) = (n)-1 ; (i) >= k ; (i)--)

using namespace std;

int main(){
  $1$0
}
